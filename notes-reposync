#!/bin/bash
#
# my reposync syntax for pulling the latest packages via 'reposync'

## for base
# reposync -gdlmnq -p /data/repos/centos7 --repoid=base --download-metadata

# for extras
# reposync -gdlmnq -p /data/repos/centos7 --repoid=extras --download-metadata

# for updates
# reposync -gdlmnq -p /data/repos/centos7 --repoid=updates --download-metadata
